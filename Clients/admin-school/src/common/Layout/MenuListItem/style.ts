import makeStyles from '@material-ui/core/styles/makeStyles';

const useStyles = makeStyles({
  sideNav: {
    overflow: 'hidden',
    padding: '10px 15px 10px 10px', 
    backgroundColor: '#623FE3',
  },
});

export default useStyles;
